#   wearables — vendor-neutral wearables dashboards.
#   Reads the Wearables data model (fed by per-vendor add-ons: TA-oura, ...).
#   Companion GA app oura_health remains the single-vendor fallback.

[install]
is_configured = 0
build = 20260720a

[package]
id = wearables

[id]
name = wearables
version = 0.1.38

[ui]
is_visible = 1
label = Wearables Health

[launcher]
author = Tony Vincent
description = Vendor-neutral wearables dashboards over the Wearables data model (sleep, heart rate, activity), fed by per-vendor add-ons such as TA-oura. Source & docs: https://github.com/narwhaldc
version = 0.1.38

[manifest]
category = Custom
